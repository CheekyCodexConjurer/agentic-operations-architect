# Detect Commands

Use this skill to infer and validate common repo commands.
1. Inspect repo signals (Makefile, package manifests, CI configs).
2. Propose install/test/lint/format/build commands.
3. Record results in `COMMANDS.md` and `COMMANDS.json`.
4. Note confidence and verification evidence.
