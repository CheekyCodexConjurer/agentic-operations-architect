# Minimal Example

This folder shows a compact, filled-out example of the agent docs.
Use it as a reference for structure and tone.

See `CLARIFY_FIRST_EXAMPLE.md` for a short planning-first dialog.
See `NEXT_CHAT_PROMPT.md` for a copy/paste continuation prompt.
